# CodeWhale 本地指令

## Git 提交流程 (强制)

每个任务完成并验证后，必须执行以下 git 流程：

1. **先拉取** — `git pull --rebase`（确保基于最新远程代码）
2. **暂存** — `git add -A`
3. **提交** — `git commit -m "..."`（描述性提交信息）
4. **推送** — `git push`

> 顺序不可颠倒。先拉后推，避免冲突。
