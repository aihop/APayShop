const n='---\ntitle: "推广体系 API"\ndescription: "用于管理推广配置、代理等级、推广成员与收益概览的后台接口。"\n---\n\n# 推广体系 API\n\n本文档描述 `promo` 模块的后台接口。这些接口均需要管理员鉴权。\n\n## 后台接口\n\n### 推广概览\n\n**接口地址：** `GET /api/admin/promo/overview`\n\n返回推广总览指标、最近代理、最近归因订单与最近收益流水。\n\n**响应示例：**\n\n```json\n{\n  "overview": {\n    "members": 12,\n    "agents": 4,\n    "masterAgents": 1,\n    "commissionAmount": 128.5,\n    "attributedOrders": 20\n  },\n  "agents": [],\n  "attributions": [],\n  "commissions": []\n}\n```\n\n### 推广配置\n\n**接口地址：** `GET /api/admin/promo/settings`\n\n返回已保存的推广配置。\n\n**响应示例：**\n\n```json\n{\n  "promo_invite_reward_amount": "10"\n}\n```\n\n**接口地址：** `POST /api/admin/promo/settings`\n\n**请求体示例：**\n\n```json\n{\n  "promo_invite_reward_amount": "10"\n}\n```\n\n### 代理等级\n\n**接口地址：** `GET /api/admin/promo/tiers`\n\n返回全部代理等级配置。\n\n**接口地址：** `POST /api/admin/promo/tiers`\n\n用于新增或更新代理等级。\n\n**请求体示例：**\n\n```json\n{\n  "code": "master-default",\n  "name": "总代理",\n  "roleScope": "master_agent",\n  "level": 100,\n  "discountRate": 0.8,\n  "salesThreshold": 0,\n  "isFixed": true,\n  "isActive": true,\n  "description": "总代理固定折扣"\n}\n```\n\n### 代理成员\n\n**接口地址：** `GET /api/admin/promo/agents?page=1&pageSize=15`\n\n返回分页的代理与总代理成员列表。\n\n**接口地址：** `POST /api/admin/promo/agents`\n\n按邮箱把现有用户设置为 `master_agent` 或 `agent`。\n\n**请求体示例：**\n\n```json\n{\n  "email": "agent@example.com",\n  "role": "agent",\n  "parentAgentUserId": 12\n}\n```\n\n### 代理关系\n\n**接口地址：** `GET /api/admin/promo/relations`\n\n返回当前代理层级关系记录，包括直属上级与总代理绑定信息。\n\n**接口地址：** `PUT /api/admin/promo/relations/:id`\n\n更新现有子代理关系的所属总代理。\n\n**请求体示例：**\n\n```json\n{\n  "parentAgentUserId": 12\n}\n```\n\n**接口地址：** `DELETE /api/admin/promo/relations/:id`\n\n禁用该代理关系，并把成员降级为普通会员。若该成员仍是带有下级的总代理，则禁止解绑。\n\n### 收益流水\n\n**接口地址：** `GET /api/admin/promo/commissions?page=1&pageSize=15`\n\n返回分页的推广收益流水列表。\n\n## 用户接口\n\n### 推广概览\n\n**接口地址：** `GET /api/users/promo/overview`\n\n返回当前用户的推荐概览、专属链接、推广码，以及 `isAgent` / `isMasterAgent` 等代理身份标记。\n当用户首次访问该接口时，后端会按需自动创建 `promo member` 记录，并生成邀请相关编码与邀请链接。\n若用户通过总代理 `agent` 链接注册，系统会先创建一条 `promo_agent_relations.status = pending` 的待审核关系；只有总代理通过后，才会进入正式团队统计。\n\n### 邀请记录\n\n**接口地址：** `GET /api/users/promo/invites`\n\n返回当前用户发起的邀请记录。\n\n### 团队成员\n\n**接口地址：** `GET /api/users/promo/team`\n\n返回当前用户可见的正式子代理团队记录，仅包含 `status = active` 的成员。\n\n### 待审核代理\n\n**接口地址：** `GET /api/users/promo/pending`\n\n返回当前总代理名下待审核的代理申请列表，仅包含 `status = pending` 的记录。\n\n**接口地址：** `POST /api/users/promo/pending/:id`\n\n用于总代理审核待加入代理申请。\n\n**请求体示例：**\n\n```json\n{\n  "action": "approve"\n}\n```\n\n`action` 支持：\n\n- `approve`：通过申请，并把该成员转为正式子代理\n- `reject`：拒绝申请，并把该待审核关系置为 `disabled`\n\n### 收益明细\n\n**接口地址：** `GET /api/users/promo/commissions`\n\n返回当前用户拥有的推广收益流水。\n\n### 团队报表\n\n**接口地址：** `GET /api/admin/promo/team-report?masterAgentUserId=12`\n\n返回指定总代理视角下的团队汇总与直属子代理销售报表。\n\n**接口地址：** `GET /api/users/promo/team-report`\n\n返回当前总代理自己的团队汇总与直属子代理销售报表。\n\n### 团队订单明细\n\n**接口地址：** `GET /api/admin/promo/team-orders?masterAgentUserId=12`\n\n返回指定总代理视角下的团队订单归因明细。\n\n**接口地址：** `GET /api/users/promo/team-orders`\n\n返回当前总代理自己的团队订单归因明细。\n';export{n as default};
//# sourceMappingURL=13.promo2.mjs.map
