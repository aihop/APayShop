export default defineAppConfig({
  ui: {
    colors: {
      primary: 'purple',
      neutral: 'zinc'
    },
    toaster: {
      defaultVariants: {
        position: 'top-center'
      }
    }
  }
})
